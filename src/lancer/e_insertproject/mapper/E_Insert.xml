<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="lancer.e_insertproject.mapper">
	<cache />

	<insert id="insertProject" parameterType="Project">
		insert into board(
		e_pr_num,e_num,p_name, p_content,p_lowerAge, p_upperAge,p_academic,p_requireNum,p_regDate,p_exDate,p_lowerCos,p_upperCos,p_startDate,p_endDate,p_location,p_experience
		)
		values(
		1, 1,#{p_name}, #{p_content}, #{p_lowerAge}, #{p_upperAge},#{p_academic},#{p_requireNum},#{p_regDate},#{p_exDate},#{p_lowerCos},#{p_upperCos},#{p_startDate},#{p_endDate},#{p_location},#{p_experience}	<!-- #변수명# 으로 사용. -->
		)
	</insert>
	
	

	



</mapper>















